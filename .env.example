# fetch_data.py 용 환경변수 템플릿.
# 실제 키를 넣어 이 파일을 '.env' 로 복사해서 사용하세요. (.env 는 커밋 금지)
#
#   cp .env.example .env
#   # 그 후 .env 를 열어 아래 값을 본인 인증키로 교체
#
# 공공데이터포털(data.go.kr) '국토교통부 아파트 매매 실거래가' 활용신청 후 발급받은 인증키.
# 인코딩 키(%2F, %2B, %3D 포함)를 그대로 넣어도 자동 디코딩됩니다.
MOLIT_API_KEY=여기에_공공데이터포털_인증키를_넣으세요
